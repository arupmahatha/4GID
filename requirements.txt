# Core data processing and analysis
pandas>=2.0.0
numpy>=1.24.0

# Database and ORM
sqlalchemy>=2.0.0

# Data generation and testing
faker>=19.0.0

# String matching and search
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0  # Optional, but improves fuzzywuzzy performance

# HTTP requests
requests>=2.31.0

# Type hints support
typing>=3.7.4

# Database driver
sqlite3  # Built into Python, no version needed

# Date and time utilities
datetime  # Built into Python, no version needed

# System and OS utilities
os  # Built into Python, no version needed
sys  # Built into Python, no version needed
re  # Built into Python, no version needed